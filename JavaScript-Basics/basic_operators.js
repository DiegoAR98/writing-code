// - Write a program to calculate the area of a rectangle (length x width).

let length = 5; // Fixed typo: 'lenght' -> 'length'
let width = 10; // Fixed typo: 'widht' -> 'width'
let rectangle = length * width;
console.log(`My rectangle area is ${rectangle}`);


// Write a program that checks if a number is positive, negative, or zero.

let number = 5;

if (number > 0) { // Removed ${} and corrected the syntax
    console.log(`${number} is greater than 0`);
} 

else if (number < 0) { // Removed ${} and corrected the syntax
    console.log(`${number} is negative`);
} 

else {
    console.log(`${number} is zero`); // Removed ${} and corrected the syntax
}



