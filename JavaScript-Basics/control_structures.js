// Write a program that calculates the perimeter of a rectangle and 
//prints whether the perimeter is even or odd.

let widht = 5
let lenght = 5
let rectangle = lenght * widht

if (rectangle % 2 === 0) {
console.log(`${rectangle} is even`)
}

else {
    console.log (`${rectangle} is odd`)
}

